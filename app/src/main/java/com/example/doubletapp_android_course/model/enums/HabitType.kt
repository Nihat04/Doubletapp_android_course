package com.example.doubletapp_android_course.model.enums

import com.example.doubletapp_android_course.R

enum class HabitType(val impactName: String) {
    POSITIVE(R.string.positive_habit.toString()),
    NEGATIVE(R.string.negative_habit.toString());
}